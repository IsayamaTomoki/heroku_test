<%= form_for @user, url: {action: :update} do |f| %>
  <p><%= f.label :name, '氏名' %>
  <%= f.text_field :name %></p>
  <p><%= f.label :pass, 'パスワード' %>
  <%= f.password_field :pass %></p>
  <%= f.submit '更新' %>
<% end %>
